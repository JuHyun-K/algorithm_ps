def solve(part, comp):
    
    part.sort()
    comp.sort()
	newPart = []
    for i in part:
        newPart.append(i)
        
    for p in newPart:
        if(p in comp):
            part.remove(p)
            comp.remove(p)
        else:
            return p




def solve(part, comp):
    part.sort(reverse=True)
    comp.sort(reverse=True)
    print(part, comp)
    name = ''
    newPart = []
    
    for i in part:
        newPart.append(i)
        
    for i in range(len(comp)-1, -1, -1):
        if(comp[i] == part[i]):
            part.remove(part[i])
            continue
        else:
            name = part[i]
            break
    if(name == ''):
        name = part[-1]
    return name